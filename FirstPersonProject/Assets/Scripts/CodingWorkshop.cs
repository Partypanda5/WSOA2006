using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CodingWorkshop : MonoBehaviour
{
    int num = 6;
    // Start is called before the first frame update
    void Start()
    {
        int score = 5;
        print(score);

        num = num + 5;

        num += 5;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

